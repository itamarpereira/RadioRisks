<h1>LLE</h1>
<p>Confira o <a href="https://pt.wikipedia.org/wiki/Lista_de_unidades_federativas_do_Brasil_por_expectativa_de_vida" target="_blank">Lista de expectativa de vida por unidades federativas do Brasil</a>.</p>

<%= simple_form_for(@solid) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :male_expected_age, label: 'Expectativa de Vida Masculina(anos)', error: 'EA(males) is mandatory, please specify one' %>
    <%= f.input :female_expected_age, label: 'Expectativa de Vida Feminina(anos)', error: 'EA(females) is mandatory, please specify one' %>
    <%= f.input :incidence, label: 'Número de casos(casos/100.000 habitantes)', error: 'Number of cases per 100.000 inhabitants' %>
  </div>

  <div class="btn btn-grad">
    <%= f.button :submit, 'Enviar' %>
  </div>
<% end %>

<%= link_to 'Back', root_path, class: "btn btn-blue" %>